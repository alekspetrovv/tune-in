application:
  title: Feed Service
  version: 1.0

server:
  port: 8087

spring:
  application:
    name: feed-service
  rabbitmq:
#    host: ${host_rabbit}
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@cluster0.k5qf3w2.mongodb.net/?retryWrites=true&w=majority
      database: comments
management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus